# Baked in at `npm run build`. Same host as the PWA, path must match nginx:
#   location ^~ /flashcards/api/ { proxy_pass http://flashcards-api:3000/; }
VITE_API_BASE_URL=https://kzt24.duckdns.org/flashcards/api
